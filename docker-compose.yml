services:
  longevin:
    build:
      context: .
      dockerfile: Dockerfile
    image: longevin:latest
    container_name: longevin
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: "3000"
      HOST: "0.0.0.0"
